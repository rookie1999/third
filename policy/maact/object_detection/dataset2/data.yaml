# data.yaml
path: /home/benson/projects/lerobot/object_detection/dataset2 # 数据集根目录的相对路径，或者完整绝对路径
train: images/train         # 训练图片路径 (相对于 path)
val: images/val              # 验证图片路径 (相对于 path)
# test: images/test            # 可选，测试图片路径 (相对于 path)

# Classes
nc: 1                        # 类别数量，根据你的实际类别数填写
names: ['object'] # 类别名称列表，顺序必须与标注时 LabelImg 生成的 classes.txt 一致
